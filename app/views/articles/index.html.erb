<% @articles.each do |article| %>
  <div class='my-2'>
    <%= link_to article.title, article_path(article), class: 'text-decoration-none fs-3' %>
    <p><%= article.content %></p>
  </div>
<% end %>
